# Runtime Archive

This folder is for the local BeeLine Excel archive.

The app creates `archive/beeline_resolved_archive.xlsx` from `templates/beeline_archive.template.xlsx` on first launch if it is missing. Do not commit runtime archive files or real resolved issue history.
