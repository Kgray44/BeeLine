# Runtime Configuration

This folder is for local BeeLine configuration.

The app creates `config/beeline_config.json` from `templates/beeline_config.template.json` on first launch if it is missing. Put real plant machine configuration here locally, and do not commit it.
