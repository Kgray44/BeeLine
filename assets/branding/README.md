# Branding Assets

BeeLine loads a logo from this folder in one consistent app header location.

- `nolato_logo_placeholder.png` is safe to commit.
- `nolato_logo.png` is ignored by Git and may be added locally only after the logo is approved for that machine/repo.
- If no approved logo is present, the app falls back to the placeholder or text-only BeeLine branding.
