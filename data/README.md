# Runtime Data

This folder is for local BeeLine SQLite runtime data only.

The app creates `data/beeline.sqlite` from `templates/beeline.template.sqlite` on first launch if it is missing. Do not commit runtime database files, real machine data, issue history, employee names, badge IDs, or plant data exports.
